import React from 'react';
import {Text,View, StyleSheet} from 'react-native';

const ResultDetail = ({item}) =>{
    return (
        <View>
            <Text>this is detail components</Text>
        </View>
    );
};

const styles=StyleSheet.create({});

export default ResultDetail;